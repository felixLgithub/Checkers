import turtle;
c = turtle.Turtle()
c.penup()
c.setpos(-200,200)
c.color("red")
c.begin_fill()
for s in range(4)
  c.forward(400);c.right(90)
  for x in range (4):
for x in range(4):
  c.begin_fill()
  for x2 in range(4):
    c.forward(50);c.right(90)
  c.end_fill()
  c.forward(100)
